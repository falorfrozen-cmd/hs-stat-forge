@AGENTS.md

# CLAUDE.md

The line above imports [`AGENTS.md`](AGENTS.md), which points at this module's
real guidance in the
[hero-siege-offline-toolkit](https://github.com/falorfrozen-cmd/hero-siege-offline-toolkit)
hub. Claude Code loads `CLAUDE.md` automatically; nearly every other coding agent
reads `AGENTS.md`. Keeping the content in one file and importing it here means
there is no second copy to drift.

**Do not add rules to this file.**
